[project]
name = "bifrost"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yqliukev", email = "yqliukev@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "google-genai>=2.22.0",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
]

[project.scripts]
bifrost = "bifrost:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
